## graphify (opcional)

Si existe `graphify-out/graph.json` y el CLI `graphify` responde rápido, puedes usarlo para preguntas de arquitectura. Si no está instalado o falla: **ignorar y usar Read/Grep/Glob**.

No bloquees implementación, pull ni fixes por graphify.
