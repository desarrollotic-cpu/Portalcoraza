# Redis local para BullMQ (motor-global) y caché futura.
# Uso: docker compose up -d redis
services:
  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
    command: ['redis-server', '--save', '', '--appendonly', 'no']
