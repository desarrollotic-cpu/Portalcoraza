# Render Static Sites NO aplican este archivo como Netlify.
# El rewrite SPA se configura en:
#   Render Dashboard → portalcoraza-web → Redirects/Rewrites
#   Source: /*   Destination: /index.html   Action: Rewrite
#
# Mientras tanto la app usa HashLocation (#/auth/login) para que las rutas funcionen.
