# Minuta Virtual completa — Plan

**Goal:** Organizar `/minutas` + reglas vigilante/registradoPor + inmutabilidad; Ops sin cambios mayores.

## Tasks

1. SQL ensure + entities + DTOs `registradoPor`
2. Service create/historial/ops resumen
3. Web: layout + rutas + formularios + historial sin PDF
4. Tests mes/DTO + build
